# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Scratch outputs
*.tmp

# Git worktrees (isolated session workspaces, e.g. .worktrees/gt-research)
.worktrees/

# Extraction scratch: text dumps / schema extracts are regenerable by
# shared/scripts/extract_text.py — but the ANALYZED artifacts (evidence
# files, master index, anexos digest) are committed work products.
sv/.extractions/*
!sv/.extractions/*.evidence.md
!sv/.extractions/00_MASTER_INDEX.md
!sv/.extractions/45_Anexos.digest.md
gt/.extractions/*
!gt/.extractions/*.evidence.md
!gt/.extractions/00_MASTER_INDEX.md

# Manual-download inbox (unverified until page-1 read + registry)
gt/sources/inbox/
hn/sources/inbox/
hn/.extractions/*
!hn/.extractions/*.evidence.md
!hn/.extractions/00_MASTER_INDEX.md
